name: mirror-source
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Mirror master branch using SRC_TOKEN for both sides
        env:
          SRC_TOKEN: ${{ secrets.SRC_TOKEN }}
        run: |
          git clone --bare "https://x-access-token:$SRC_TOKEN@github.com/balsicl1234/9router.git" /tmp/mirror
          cd /tmp/mirror
          git push "https://x-access-token:$SRC_TOKEN@github.com/balsickass/9router-backup.git" "+refs/heads/master:refs/heads/master" --force 2>&1 | tail -8
          echo "done"
